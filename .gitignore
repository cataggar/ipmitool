# Zig build system (see build.zig)
zig-out/
.zig-cache/

# golden test harness scratch root (see doc/zig-migration/golden-harness.md).
# The harness removes it on exit; this covers interrupted runs. Under
# `zig build test` the scratch root lives in .zig-cache instead.
.golden-work/

# scratch output of tools/dump_strings.sh and tools/diff_cli.sh
build/

# autotools build (kept as a cross-check during the Zig migration)
.deps
.dirstamp
.libs
.*.swp
*.log
*.rej
*.orig
*.o
*.lo
*.la
*.*~
*.tar.gz
*.tar.bz2
Makefile
Makefile.in
aclocal.m4
ar-lib
autom4te.cache
compile
config.guess
config.h
config.h.in
config.h.in~
config.log
config.status
config.sub
configure
configure~
depcomp
install-sh
ltmain.sh
libtool
missing
stamp-h1
test-driver
control/ipmitool.spec
control/pkginfo
control/prototype
control/rpmmacros
src/ipmievd
src/ipmitool
doc/ipmievd.8
doc/ipmitool.1
enterprise-numbers

# editors and tooling
cscope.out
tags
