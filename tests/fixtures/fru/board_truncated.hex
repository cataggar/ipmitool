# A FRU image that stops in the middle of the board info area even though the
# common header and the area length byte claim there is more. Exercises the
# bounds checks in fru_area_print_board().

@fru/board.hex
@truncate 48          # cut 16 bytes into the 64-byte board area
