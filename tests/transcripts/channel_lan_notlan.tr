# Channel 1 reports the RS-232 medium, so ipmi_get_channel_medium() returns
# 05h and is_lan_channel() rejects the channel before any Set Channel Access
# request is sent.
default_ccode 0xc1

respond get_channel_info
  netfn 0x06
  cmd 0x42
  data 01 05 04 00 bc 0a 00 ff ff
end
