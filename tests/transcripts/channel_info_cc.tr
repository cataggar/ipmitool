# Get Channel Info fails with a completion code, so the ccode is positive and
# eval_ccode() prints "IPMI command failed".
default_ccode 0xc1

respond get_channel_info
  netfn 0x06
  cmd 0x42
  ccode 0xd4
end
