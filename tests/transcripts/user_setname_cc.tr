# Set User Name fails with 0xD4, exercising the branch that appends the
# decoded completion code to the error message.
default_ccode 0xc1

respond set_user_name
  netfn 0x06
  cmd 0x45
  ccode 0xd4
end
