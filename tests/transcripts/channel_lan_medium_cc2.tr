# Get Channel Info fails with CCh (invalid data field in request), the one
# completion code ipmi_get_channel_medium() swallows silently.
default_ccode 0xc1

respond get_channel_info
  netfn 0x06
  cmd 0x42
  ccode 0xcc
end
