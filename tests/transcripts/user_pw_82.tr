# Set User Password answers 0x82, which is neither 0, 0x80 nor 0x81, so
# `user test` reports the catch-all "Unknown error" and still exits non-zero.
default_ccode 0xc1

respond set_user_password
  netfn 0x06
  cmd 0x47
  ccode 0x82
end
