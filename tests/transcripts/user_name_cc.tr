# Get User Name fails with 0xD5, which is *not* the 0xCC that the walk
# tolerates, so the listing aborts.
default_ccode 0xc1

respond get_user_access
  netfn 0x06
  cmd 0x44
  data 04 41 01 34
end

respond get_user_name
  netfn 0x06
  cmd 0x46
  ccode 0xd5
end
